    const obj = {
        nombre: "Televisor de 80 pulgadas",
        precio: 300,
        disponible: true
    }

    // const nombre = obj.nombre
    // console.log(nombre)

    const { nombre, disponible } = obj
    console.log(nombre, disponible)